# ReactJS Sliding Menu
Sliding menus using ReactJS.

## Setup
Install yarn packages
```bash
yarn install
```
Start React app
```bash
yarn start
```
The webpage should be running on [http://localhost:3000](http://localhost:3000).
